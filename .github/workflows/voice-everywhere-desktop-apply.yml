name: Apply Realtime desktop route patch

on:
  push:
    branches:
      - feat/voice-everywhere

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
      - name: Apply staged desktop Realtime patch
        shell: bash
        run: |
          set -euo pipefail
          if [ "$(git log -1 --pretty=%s)" != "chore: stage realtime desktop patch" ]; then
            exit 0
          fi
          cat .voice-everywhere-desktop/part* > /tmp/voice-everywhere-desktop.patch
          git apply --check /tmp/voice-everywhere-desktop.patch
          git apply --index /tmp/voice-everywhere-desktop.patch
          git diff --cached --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "feat(voice): wire paired routes through Realtime desktop"
          git push origin HEAD:feat/voice-everywhere
